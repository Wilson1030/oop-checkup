# 预注册 v3 · 补齐结构类检查项

> 前置：v1 `13b2284`（失败）、v2 `1fa106e`（检查项1通过 / 检查项2失败）、RUN-002 `48a7074`。
> 本文件在实现检查项 3/4/5/6 之前提交。

---

## 〇、本轮三处变更及理由

### 变更 1 · 检查项 2 降级为「待确认」

RUN-002 证明其判据是**语义性**的（「这组参数能否命名一个有意义的概念」），
纯静态分析无法达到可用准确率。

新形态：不再断言「违反」，改为列出候选并把判断权交还学生。

```
? 待确认  (String ip, String port)   跨 3 个类、7 个方法
  这两个参数总是一起出现。你能给它们起一个名字吗？
  起得出来 → 它们就该是一个类
  起不出来 → 忽略本条
```

**这不是逃避，教学上反而更好**：起名这个动作本身就是在逼学生判断
「这几个东西是不是同一个概念」——那正是我们希望他学会的思考。

「待确认」不计入违反数，不参与证伪条件判定。

### 变更 2 · 检查项 6 重新定义

原定义「类与方法的粒度（Large Class / Long Method）」存在两个问题：

1. **不贴题**：方法过长是通用代码质量问题，与「C 思维残留」只有间接关系
2. **必然误报**：成熟库中长方法普遍存在，会重蹈检查项 2 的覆辙

改为聚焦 C 思维最典型的表现形态：

> **检查项 6 · 入口方法是否过度承担**
> 所有逻辑都塞在 `main` 里 —— 这是「用 Java 写 C 程序」最直白的证据。

副作用：库代码没有 `main`，天然不会误报。

### 变更 3 · 修正检查项 1 的 E2 判据

v2 写为「`private static` 嵌套类」，实际 Guava `ValueHolder` 是 `static class`（包私有）。
修正为「**非 public 的 static 嵌套类**」。属描述错误修正，已在 RUN-002 分析中留痕。

---

## 一、新增判据（冻结）

### 检查项 3 · 是否用多态替代类型判断
标准：Switch Statements｜出处：Fowler《重构》坏味道 #11

| 判据 | 定义 |
|---|---|
| A | 同一 if-else 链中出现 ≥ 2 个 `instanceof` |
| B | 同一组 `case` 标签集合在 ≥ 2 处 `switch` 中重复出现 |
| C | 同一组 `instanceof` 类型集合在 ≥ 2 个方法中重复出现 |
| 排除 | `equals()` 方法内的 `instanceof`（Java 规范要求的标准写法） |
| 🔴 严重 | 触发 B 或 C —— 重复出现意味着新增一个类型要改多处 |
| 🟡 中等 | 仅触发 A |

**正当性（独立于样本）**：Fowler 对该坏味道的核心论述是「基于类型码的条件分派，
新增类型时必须修改所有分派点」。**单处**分派可能是合理的（菜单、状态机），
故只在**重复出现**时判为严重。

### 检查项 4 · static 是否被滥用
标准：全局状态破坏封装｜出处：Java 教科书通识

| 判据 | 定义 |
|---|---|
| A | 存在 `static` 非 `final` 字段 —— 即全局变量 |
| B | 非工具类中 `static` 方法占比 > 50% 且 `static` 方法数 ≥ 5 |
| 排除 | 类名 `*Utils` / `*Helper` / `*Constants` 且无实例字段 |
| 排除 | `private static` 且类型为自身类型的字段（单例惯例） |
| 🔴 严重 | 触发 A 且该字段非 private |
| 🟡 中等 | 其余 |

**正当性**：`static` 非 `final` 字段在语义上等同于 C 的全局变量，
这是任何一本 Java 教科书都明确反对的。判据极硬，几乎不存在解释空间。

### 检查项 5 · 封装是否完整
标准：public 可变字段｜出处：Fowler《重构》坏味道 #5 关联

| 判据 | 定义 |
|---|---|
| A | 存在 `public` 非 `final` 非 `static` 实例字段 |
| 排除 | 沿用检查项 1 的 DTO/VO/Entity 命名与注解排除 |
| 🔴 严重 | 同一类中 ≥ 3 个 |
| 🟡 中等 | 1–2 个 |

**正当性**：`public` 可变字段直接暴露内部表示，是封装破损的教科书定义。
本判据是全部七项中最硬的一条——成熟库中不应存在。

### 检查项 6 · 入口方法是否过度承担
标准：Long Method 的特化｜出处：Fowler《重构》坏味道 #6

| 判据 | 定义 |
|---|---|
| A | `main` 方法体 > 50 行 |
| 🔴 严重 | > 100 行 |
| 🟡 中等 | 50–100 行 |

**正当性**：把全部逻辑写在 `main` 里，是「用 Java 写 C 程序」最直白的形态。
50/100 行的阈值取自业界惯例（Checkstyle 默认 150、SonarQube 100），本工具取值更严，
因为面向的是教学场景下的小型程序。

---

## 二、预测（在实现之前写下）

| 样本 | 项3 多态 | 项4 static | 项5 封装 | 项6 main |
|---|---:|---:|---:|---:|
| guava | 0 – 2 | 0 – 2 | **0** | **0** |
| junit5 | 0 – 2 | 0 – 2 | **0** | **0** |
| buaa-oo | 0 – 3 | 1 – 4 | 0 – 3 | 0 – 2 |
| lr580-hw | 0 – 3 | 0 – 4 | 0 – 5 | 0 – 2 |
| minigame | 0 – 2 | 0 – 4 | 0 – 5 | 0 – 1 |
| vinamdar-oop | 0 – 1 | 0 – 2 | 0 – 2 | 0 – 1 |

项 5 与项 6 在 A 组的预测是**硬性的 0**：
成熟库不应有 public 可变字段，也不含 `main`。

---

## 三、证伪条件

| 编号 | 条件 | 含义 |
|---|---|---|
| **H1** | A 组任一结构类检查项（3/4/5/6）检出 ≥ 5 | 仍在误报好代码 |
| **H2** | A 组检查项 5 或 6 检出 > 0 | 最硬的两条判据都误报，说明实现有 bug |
| **H3** | B 组四样本「违反项数」平均 < 2 项 | **信号过弱**，报告撑不起「扎心」 |
| **H4** | 逐条核验准确率 < 80% | 误报率过高 |

H1、H2 为硬性终止条件。
**H4 本轮必须实际执行**——RUN-002 因 G1 提前触发而未做，本轮不得再跳过。

---

## 四、防作弊状态声明

样本已在磁盘上，且我已看过 RUN-001 / RUN-002 的输出，「顺序锁」不再有效。
本轮仅剩：

1. **预注册锁**——本文件先于实现提交，预测值已写死
2. **判据正当性锁**——上述四条判据均可脱离样本独立论证；
   若出现「阈值恰好卡在两组数据之间」的情形（如 RUN-002 中被我拒绝采用的
   「最大单类占比 < 60%」），一律视为作弊，不得采用

RUN-002 中我已明确拒绝过一次事后调参，本轮延续该标准。
