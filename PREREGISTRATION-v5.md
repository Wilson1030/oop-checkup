# 预注册 v5 · 检查项 3/4 修正 + 检查项 6 验证

> 前置：v4 `0ca414d`、RUN-004 `d7a3724`（准确率 25/31 = 80.6%）。
> 本文件在修改代码与下载新样本之前提交。

---

## A · 检查项 3、4 的修正

### M1 · 检查项 3 只统计对「项目自定义类型」的 instanceof

排除 JDK 内置类型与数组类型，仅保留项目内声明过的类型。
实现方式：以项目中所有类声明的名字构成集合，`instanceof T` 中的 `T`
必须在该集合内且非数组类型；过滤后若剩余类型 < 2，则该处不再计入。

**正当性（独立于样本）**：
> 多态只能用于**你能修改的类型**。
> 对 `String`、`Collection`、`boolean[]` 做 instanceof 是无法避免的
> ——你无法给它们添加方法。指出这类分派没有任何可行动性。

### M3 · 检查项 4 排除静态工厂方法

计算 static 方法占比时，排除返回类型中包含本类名的 static 方法
（覆盖 `DynamicTest xxx()` 与 `Stream<DynamicTest> xxx()` 两种形态）。

**正当性**：静态工厂方法是公认模式（Effective Java 第 1 条），不属 static 滥用。

### M2 · 不实施（明确记录）

RUN-004 中拟议的「排除状态机 switch」**本轮不做**。

理由：区分「状态码」与「类型码」本质上是语义判断。
若改用「排除纯数字字面量 case」这类结构近似，会误伤 C 风格类型码
（`switch(type) { case 0: … }` 恰恰是学生代码中真实存在的形态）——
**为了消灭一个假阳性而制造假阴性，不划算。**

因此 guava 的 `AbstractIterator.switch(state)` 误报将**继续存在**，作为已知局限记录在案。

---

## B · 检查项 6 的验证

RUN-004 中检查项 6 在 6 个样本上全部为 0，**从未触发**，正确性零证据。

### B1 · 最小测试用例（验证实现）

在 `testdata/MainBloat/` 下手写一个 `main` 超过 60 行的 Java 文件。

**这不是样本验证，是单元测试**——它回答「代码写对了吗」，
不回答「真实世界里存在吗」。两个问题必须分开。

预期：检查项 6 报出 1 处「中等」。
若不报，说明实现有 bug。

### B2 · 新学生样本（盲测）

新增 4–6 个此前未接触过的学生仓库。

**流程约束**：下载后**不查看任何源码**，直接运行。
新样本对全部检查项而言都是盲的，「顺序锁」在这些样本上重新生效
——这是自 RUN-001 以来首次恢复该保护。

搜索条件只使用元信息（关键词、语言、star 数），不阅读代码内容。

---

## 二、预测（写死于实现之前）

### 现有 6 个样本 · 检查项 3

| 样本 | RUN-004 | **v5 预测** | 依据 |
|---|---:|---:|---|
| guava | 3 | **1 – 2** | M1 消除 `CharSequence/Collection/Map`；`MemoizingSupplier` 是 Guava 自有类型，仍会保留；`switch(state)` 因 M2 不实施而保留 |
| junit5 | 2 | **0** | `boolean[]` 等为数组类型、`Iterable` 为 JDK 类型，全部消除 |
| buaa-oo | 1 | **1** | `ElevatorRequest`/`MaintainRequest` 是项目自有类型，应保留 |
| lr580-hw | 0 | **0** | |
| minigame | 0 | **0** | |
| vinamdar | 0 | **0** | |

### 现有 6 个样本 · 检查项 4

| 样本 | RUN-004 | **v5 预测** |
|---|---:|---:|
| guava | 1 | **1**（`SystemLoader.disabled`，真阳性，不受 M3 影响） |
| junit5 | 1 | **0**（`DynamicTest` 为静态工厂，被 M3 排除） |
| 其余四个 | 0/10/0/2 | **不变** |

### 其他检查项

检查项 1、2、5、6 代码不变，**预期与 RUN-004 完全一致**。

### 准确率

修正后 A 组预计剩余 3 条（guava 2 + junit5 0 + guava 项4 1），其中 1 条为真。

> **总体准确率预测：≥ 90%**（约 25 真 / 27 条）

### 检查项 6

| 对象 | 预测 |
|---|---|
| B1 最小测试用例 | **必须报出 1 处**，否则实现有 bug |
| B2 新学生样本 | **至少 1 个样本触发**（main > 50 行） |

---

## 三、证伪条件

| 编号 | 条件 | 含义 |
|---|---|---|
| **J1** | A 组任一结构类检查项 ≥ 5 | 仍在误报好代码 |
| **J2** | A 组检查项 5 或 6 > 0 | 最硬的两条出现误报 |
| **J3** | 检查项 1/2/5 结果与 RUN-004 不一致 | 修改产生非预期副作用 |
| **J4** | B1 最小用例未报出检查项 6 | **实现有 bug** |
| **J5** | 逐条核验准确率 < 90% | 修正未达预期效果 |
| **J6** | B2 新样本上出现**新类型**的误报 | 此前的样本覆盖不足，判据仍不完善 |

J1–J4 为硬性终止条件。
**J6 是本轮最有价值的检验**——新样本是盲的，它能发现前四轮因反复看同一批样本而形成的过拟合。
