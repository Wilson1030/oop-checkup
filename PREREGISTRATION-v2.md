# 预注册 v2 · oo-checkup 阶段 0（第二轮）

> v1 判定失败（见 `runs/RUN-001-ANALYSIS.md`，F1+F2 双触发）。
> 本文件在修改任何规则代码之前写成并提交。
> 前置提交：预注册 v1 `13b2284`、引擎 v1 `afc3d4f`、RUN-001 结果 `f427e4c`。

---

## 〇、与 v1 相比的三处根本变更

| | v1 | v2 |
|---|---|---|
| 产品形态 | 加权总分 + 雷达图 | **检查表**，逐条通过/违反，不给总分 |
| 判定依据 | 我自定的阈值 | **公认标准 + 文献出处** |
| 验证方法 | A/B 组统计对比 | **逐条准确率核验** |

### 为什么废除总分

总分隐含"存在一把普适的尺子"，而这把尺子只在 L4（具体项目该怎么设计）层面才需要，那一层没有标准。
检查表只断言 L1–L3（语言语义 / 设计原则 / 坏味道清单），这三层有公认标准。

### 为什么改验证方法

v1 拿 Guava 和学生作业对比，混淆变量太多（领域、规模、目的、硬约束全不同），
且 B 组没有 ground truth —— 我是用工具输出反推"学生代码差"，属于循环论证。

检查表把验证单位从「项目」降到「单条发现」：
不必回答"这个项目比 Guava 好还是差"，只需回答"这一条是不是真的违反了那条标准"。
后者每条独立可判、有文献可依，且 **Guava 的 69 次重载在这个框架下直接就是假阳性**
——方法重载不在任何坏味道清单里，一眼判死，不需要统计。

---

## 一、完整检查表（7 项）

| # | 检查项 | 违反的标准 | 出处 | 阶段0 |
|---|---|---|---|:---:|
| 1 | 数据与行为是否结合 | Anemic Domain Model | Fowler 2003 专文 | ✅ |
| 2 | 是否避免散装参数传递 | Data Clump / Long Parameter List | 《重构》坏味道 #3 #4 | ✅ |
| 3 | 是否用多态替代类型判断 | Switch Statements | 《重构》坏味道 #11 | 阶段1 |
| 4 | static 是否被滥用 | 全局状态破坏封装 | Java 教科书通识 | 阶段1 |
| 5 | 封装是否完整 | Feature Envy / public 可变字段 | 《重构》坏味道 #5 | 阶段1 |
| 6 | 类与方法的粒度 | Large Class / Long Method | 《重构》坏味道 #1 #6 | 阶段1 |
| 7 | 是否避免基本类型偏执 | Primitive Obsession | 《重构》坏味道 #2 | 阶段1 |

**阶段 0 只实现第 1、2 项。**

---

## 二、冻结的新判据

### 检查项 2 · 散装参数传递（原 R1）

一个参数组合必须**同时**满足以下全部条件，才计为违反：

| # | 条件 | 理由 |
|---|---|---|
| C1 | 长度 ≥ 2 的连续参数子序列，类型与名称均一致 | 沿用 v1 |
| C2 | **出现在 ≥ 2 个不同的类中** | 同一类内部的辅助方法链共享上下文是正常设计 |
| C3 | **涉及 ≥ 2 个不同的方法名** | 方法重载家族是 API 设计，不是坏味道 |
| C4 | 出现次数 ≥ 2 | 沿用 v1 |
| C5 | 非全部单字母参数名 | 沿用 v1 |

严重度：
- 🔴 跨 ≥ 3 个类 **或** 出现 ≥ 5 次
- 🟡 其余满足条件者

**C2 与 C3 的正当性独立于样本**：它们是从「参数团」这个概念本身推出的
——参数团的危害在于「同一组数据被拆散后跨越多个不相关的地方传递」，
而同类内辅助方法链和同名重载都不具备这个特征。

回溯验证（依据 RUN-001 已观察到的数据）：

| 案例 | C2 跨类 | C3 非重载 | 结果 |
|---|:---:|:---:|---|
| Guava `Preconditions.checkArgument` ×69 | ❌ 同一类 | ❌ 同一方法名 | 排除 |
| JUnit5 `AssertArrayEquals` ×28 | ❌ 同一类 | ❌ 同一方法名 | 排除 |
| 学生 `(ip, port)` ×7 | ✅ 3 个类 | ✅ 7 个方法名 | **保留** |

### 检查项 1 · 数据与行为分离（原 R2）

判据不变，补两条排除（属修 bug，非调参）：

| # | 新增排除 | 理由 |
|---|---|---|
| E1 | `@interface`（注解声明） | 注解不是类，没有行为是其语言定义；RUN-001 中 JUnit5 的 3 项误报全部属此 |
| E2 | `private static` 嵌套类 | 内部数据节点贫血是刻意设计；RUN-001 中 Guava `MoreObjects.ValueHolder` 属此 |

### 规模自适应

沿用 v1，不变。

---

## 三、预测（在改代码之前写下）

### 检查项 2（散装参数）

| 样本 | v1 实际 | **v2 预测** |
|---|---:|---:|
| guava | 82 | **0** |
| junit5 | 255 | **0 – 2** |
| buaa-oo | 1 | 0 – 2 |
| lr580-hw | 13 | **1 – 4** |
| minigame | 1 | 0 – 2 |
| vinamdar-oop | 0 | 0 – 1 |

### 检查项 1（数据与行为分离）

| 样本 | v1 实际 | **v2 预测** |
|---|---:|---:|
| guava | 1 | **0** |
| junit5 | 3 | **0** |
| buaa-oo | 2 | 2 |
| lr580-hw | 0 | 0 |
| minigame | 2 | 2 |
| vinamdar-oop | 0 | 0 |

### 准确率

对**全部**检出逐条人工核验（预计总数不足 20 条，无需抽样）：

> **真实违反数 / 总检出数 ≥ 80%**

---

## 四、证伪条件

| 编号 | 条件 | 含义 |
|---|---|---|
| **G1** | A 组（guava/junit5）任一检查项检出 ≥ 3 | 排除逻辑无效，仍在误报好代码 |
| **G2** | 逐条核验准确率 < 80% | 误报率过高，不可用 |
| **G3** | B 组四个样本检出总数 = 0 | 判据收得过紧，什么都测不出 |
| **G4** | B 组平均检出 < 1 项 | **信号过弱** —— 报告撑不起「扎心」，两条规则不足以支撑产品，必须补齐检查项 3–7 |

**G1 与 G2 为硬性终止条件。**
G4 不是终止条件，而是**规模判断**：触发即说明阶段 0 的两条规则不足以验证产品价值，
必须先补完 7 项检查表才能回答「报告扎不扎心」。

---

## 五、本轮防作弊机制的削弱（必须声明）

v1 有「顺序锁」——先写代码、冻结阈值，再下载样本。
**v2 不再具备这个保护**：样本已在磁盘上，RUN-001 的输出我已经看过。

本轮只剩两道锁：

1. **预注册锁**　本文件先于代码修改提交，预测值已写死。
2. **判据正当性锁**　C2（跨类）与 C3（非重载）的正当性可以脱离样本独立论证
   ——它们直接来自「参数团」的定义，而非为了让 Guava 好看而设。
   若我引入的是「排除类名含 Preconditions 的类」这种针对性规则，那才是作弊。

这个削弱是真实存在的，读者应据此打折看待 v2 的结果。

---

## 六、范围

本轮**只做学生端**。老师端（批量分析、班级报告、CSV 导出）全部推迟。
`--batch` 参数保留，但仅作为验证用的开发工具，不作为产品功能。
