# 预注册 v6 · N1 / N2 修正

> 前置：v5 `9a06755`、RUN-005 `17f1cac`（J5+J6 触发，准确率 46/55 = 83.6%）。
> 本文件在修改代码之前提交。

---

## 一、N1 · 检查项 3：switch 仅在标签为具名标识符时检测

若一处 `switch` 的全部 `case` 标签（不含 `default`）都是纯数字字面量
或纯数字字符串（`case 1:`、`case "0":`），则不计入检测。

**正当性（独立于样本）**：
> 一个「类型」必须有名字才能表达概念。`case ById:` 说明 `ById` 是一个有意义的分类，
> 值得为它建立一个类；而 `case "1":` 中的 `"1"` 不代表任何领域概念 —— 它是序号。
> **对一个没有名字的数字做多态改造是不可能的，因为造不出对应的类。**

**关于 v5 中拒绝 M2 的理由的修正**：
v5 担心「C 风格 `switch(type){case 0:}` 类型码会被漏掉」。正确的回应是：
若类型码连名字都没有，第一步应当是**给它命名（引入枚举）**，而不是引入多态。
该情形属于检查项 7（基本类型偏执）的职责范围，不属检查项 3。

## 二、N2 · 检查项 4：移除判据 B

检查项 4 仅保留判据 A（存在 `static` 非 `final` 字段）。

**依据（纯证据，无需论证）**：判据 B 三轮的全部产出

| 轮次 | 检出 | 判定 |
|---|---|---|
| RUN-003 | guava 12 条、junit5 16 条（工具类） | 全部误报 |
| RUN-004 | junit5 `DynamicTest`（静态工厂） | 误报 |
| RUN-005 | book-console `BookQuery`、`BorrowRecordQuery`（DAO） | 误报 |

> **30 条检出，0 条正确。**检查项 4 的全部真阳性均来自判据 A。

---

## 三、预测（写死于实现之前）

### 检查项 3

| 样本 | RUN-005 | **预测** | 依据 |
|---|---:|---:|---|
| guava | 2 | **1** | `switch[0,1,2]` 为数字标签被排除；`instanceof[MemoizingSupplier]` 保留 |
| book-console | 2 | **1** | 菜单 `["1","2"]` 排除；`[ByBookname, ById]` **必须保留** |
| stu-mgmt-szpiss | 2 | **1** | 菜单排除；`instanceof[Admin,Course,Score,Student]` **必须保留** |
| stu-score | 3 | **0** | 三条全是菜单 |
| 其余 9 个样本 | 0 | **0** | |

### 检查项 4

| 样本 | RUN-005 | **预测** | 依据 |
|---|---:|---:|---|
| book-console | 5 | **3** | 移除 `BookQuery`、`BorrowRecordQuery` 两条判据 B 检出 |
| lr580-hw | 10 | **10** | 全部来自判据 A |
| stu-score | 4 | **4** | 同上 |
| bank-rehan | 3 | **3** | 同上 |
| guava / bank-hitdepani / szpiss / vinamdar | 1/1/1/2 | **不变** | 同上 |
| 其余 | 0 | **0** | |

### 其他

检查项 1、2、5、6 代码不变，**预期与 RUN-005 完全一致**。

### 准确率

> **≥ 95%**（预计 46 真 / 47 条 ≈ 97.9%）

---

## 四、证伪条件

| 编号 | 条件 | 含义 |
|---|---|---|
| **K1** | A 组任一结构类检查项 ≥ 5 | 仍在误报好代码 |
| **K2** | A 组检查项 5 或 6 > 0 | 最硬的两条出现误报 |
| **K3** | 检查项 1/2/5/6 与 RUN-005 不一致 | 修改产生非预期副作用 |
| **K4** | **真阳性被误删** —— `book-console` 的 `[ByBookname, ById]` 或 `stu-mgmt-szpiss` 的 `instanceof[Admin, Course, Score, Student]` 消失 | **修正过头，制造了假阴性** |
| **K5** | 逐条核验准确率 < 95% | 未达预期效果 |

K1–K4 为硬性终止条件。

**K4 是本轮最重要的检验**：前五轮的失败模式都是「误报」，
而修正误报的最大风险是矫枉过正，把真信号一起滤掉。
本轮必须同时确认两条已知真阳性存活。
