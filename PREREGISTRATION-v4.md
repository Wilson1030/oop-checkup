# 预注册 v4 · 检查项 4 判据修正

> 前置：v3 `711f4b8`、RUN-003 结果已提交。
> 本文件在修改检查项 4 的代码之前提交。

---

## 一、RUN-003 结果对照

| 样本 | 项3 预测/实际 | 项4 预测/实际 | 项5 预测/实际 | 项6 预测/实际 |
|---|---|---|---|---|
| guava | 0–2 / **3** ⚠️ | 0–2 / **12** ❌ | **0 / 0** ✅ | **0 / 0** ✅ |
| junit5 | 0–2 / **2** ✅ | 0–2 / **16** ❌ | **0 / 0** ✅ | **0 / 0** ✅ |
| buaa-oo | 0–3 / 1 ✅ | 1–4 / 0 ❌ | 0–3 / 2 ✅ | 0–2 / 0 ✅ |
| lr580-hw | 0–3 / 0 ✅ | 0–4 / **15** ❌ | 0–5 / 3 ✅ | 0–2 / 0 ✅ |
| minigame | 0–2 / 0 ✅ | 0–4 / 0 ✅ | 0–5 / 2 ✅ | 0–1 / 0 ✅ |
| vinamdar | 0–1 / 0 ✅ | 0–2 / 2 ✅ | 0–2 / 0 ✅ | 0–1 / 0 ✅ |

| 证伪条件 | 状态 |
|---|---|
| **H1** A 组结构类检查项 ≥ 5 | 🔴 **触发**（项4：guava 12、junit5 16） |
| **H2** A 组项 5 或项 6 > 0 | ✅ **未触发** —— 最硬的两条判据零误报 |
| H3 B 组平均违反项数 < 2 | 未触发（均值 2.5 项） |
| H4 逐条准确率 | 待项 4 修正后执行 |

**判定：检查项 5、6 通过；检查项 3 边缘（超预测 1，未触发 H1）；检查项 4 失败。**

---

## 二、检查项 4 的失败原因

A 组 28 项检出中，**全部**来自判据 B（static 方法密度），且全部是同一类东西：

```
Guava   : Ascii、Enums、Functions、Platform、Preconditions、
          Predicates、Strings、Suppliers …  均为 100% static
JUnit5  : AssertAll、AssertEquals、Assertions、AssertArrayEquals …
          均为 100% static
```

这些是**标准工具类**。全 static 是正确设计——`java.lang.Math`、
`java.util.Arrays`、`java.util.Collections` 都是同一形态。

失效原因：v3 的工具类排除判据是「**类名匹配 `*Utils`/`*Helper`/`*Constants`**」，
而这些类叫 `Ascii`、`Strings`、`Preconditions`，名字不匹配。

**名字不是工具类的本质特征。**

---

## 三、修正（两处，均为概念定义修正，非阈值调整）

### 修正 1 · 工具类的判据

| | v3 | **v4** |
|---|---|---|
| 判据 | 类名后缀匹配 **且** 无实例字段 | **无实例字段 且 无实例方法** |

**正当性（独立于样本）**：
一个类如果既没有实例字段、也没有实例方法，那它从设计上就不打算成为对象——
它是一个函数集合（命名空间）。这在 Java 中是合法且标准的写法。
`java.lang.Math` 不叫 `MathUtils`，但没有人会说它「滥用 static」。

判据依据的是**类的结构**，不是它的名字。

### 修正 2 · 判据 B 的语义

| | v3 | **v4** |
|---|---|---|
| 判据 B | 非工具类中 static 方法占比 > 50% 且 static 方法 ≥ 5 | **类有 ≥ 1 个实例字段**，且 static 方法占比 > 50%，且 static 方法 ≥ 5 |
| 本质 | 密度检测 | **矛盾检测** |

**正当性（独立于样本）**：
判据 B 真正要捕捉的信号是**自相矛盾的设计**——
类声明了实例字段（说明它想成为一个对象），却几乎不用实例方法去操作这些字段
（说明它并没有真的把自己当对象用）。

若类完全没有实例字段，那它要么是工具类（合理），
要么是「用 static 字段模拟全局对象」——而后者已由**判据 A**（static 非 final 字段）捕捉。
判据 B 不应重复覆盖。

### 修正的连带影响（提前声明）

`lr580-hw` 的 `DbCtrl`（22 个方法全 static、17 个 static 可变字段）
将不再由判据 B 报出，改为仅由判据 A 报出「17 个全局变量」。

**这是更准确的描述**：它不是「static 用得太多」，它是「用 static 字段模拟了一个全局对象」。

---

## 四、这是修正还是调参？—— 自我审查

| | 作弊的形态 | 本次修正 |
|---|---|---|
| 手法 | 看着两组数字，把阈值卡在中间 | 修改判据的**概念定义** |
| 可否独立论证 | 否（换批样本即失效） | **是**——`java.lang.Math` 的例子不依赖本次样本 |
| 参照 | RUN-002 中被我拒绝的「最大单类占比 < 60%」 | 与之性质不同 |

判定：**属正当修正**。但仍须先写死新预测再跑，不得改完直接看结果。

---

## 五、新预测（检查项 4）

| 样本 | v3 实际 | **v4 预测** | 依据 |
|---|---:|---:|---|
| guava | 12 | **0 – 2** | 工具类全部排除；base 包应无 static 可变字段 |
| junit5 | 16 | **0 – 2** | 同上 |
| buaa-oo | 0 | **0 – 2** | 判据 A 可能捕捉到 `Global` 类 |
| lr580-hw | 15 | **3 – 6** | `DbCtrl`/`TbGlobal`/`DbLoader` 三个类的全局变量 |
| minigame | 0 | **0 – 2** | |
| vinamdar-oop | 2 | **1 – 2** | 判据 A 已捕捉，不受本次修正影响 |

其余检查项（1、2、3、5、6）代码不变，预期结果与 RUN-003 完全一致。
**若出现变化，说明修改产生了非预期的副作用。**

---

## 六、证伪条件（RUN-004）

| 编号 | 条件 | 含义 |
|---|---|---|
| **I1** | A 组任一结构类检查项（3/4/5/6）检出 ≥ 5 | 仍在误报好代码 |
| **I2** | A 组项 5 或项 6 > 0 | 最硬的两条判据出现误报 |
| **I3** | 检查项 1/2/3/5/6 的结果与 RUN-003 不一致 | 修改产生了非预期副作用 |
| **I4** | B 组平均违反项数 < 2 项 | 信号过弱 |
| **I5** | 逐条核验准确率 < 80% | 误报率过高 |

I1、I2、I3 为硬性终止条件。
**I5 本轮必须实际执行**——已连续两轮因提前触发而跳过，不得再跳。
