# 预注册 · oo-checkup 阶段 0

> **本文件在编写任何检测代码之前写成并提交。**
> 目的：把规则阈值和预期结果固定在纸面上，使事后调参无法伪装成"本来就是这样"。
> 提交时间以 git commit 时间戳为准。

---

## 一、四条承诺

1. **顺序锁**　本文件提交后才开始写检测代码；检测代码写完、阈值冻结后才下载样本。
2. **样本锁**　样本全部来自第三方（开源项目 + GitHub 真实学生作业），不使用任何由 AI 生成的样本。
3. **公开锁**　首次运行结果原样公开，不论多难看。
4. **留痕锁**　此后任何阈值调整必须记录：改了哪个数、为什么改、改前改后各样本的检出数分别是多少。

---

## 二、冻结的规则定义

### R1 · 参数团（Data Clump）

| 项 | 定义 |
|---|---|
| 检测对象 | 所有方法与构造器的参数列表 |
| 提取方式 | 取长度 ≥ 2 的**连续**参数子序列，以 `(类型,名称)` 序列为键 |
| 计数范围 | 整个被分析项目 |
| 🔴 严重 | 同一组合出现 **≥ 3 次** |
| 🟡 中等 | 同一组合出现 **= 2 次** |
| 排除 | `main(String[])`、`equals(Object)`、以及全部参数名为单字母的组合 |

### R2 · 贫血模型（Anemic Domain Model）

| 项 | 定义 |
|---|---|
| 触发条件 | 类的**实例字段数 ≥ 3** 且 **业务方法数 = 0** |
| 业务方法 | 排除 getter / setter / 构造器 / `toString` / `equals` / `hashCode` / `compareTo` 之后剩下的方法 |
| getter 判定 | 名称以 `get`/`is` 开头 且 无参数 且 有返回值 且 方法体 ≤ 3 行 |
| setter 判定 | 名称以 `set` 开头 且 恰好 1 个参数 且 返回 `void` 且 方法体 ≤ 3 行 |
| 🔴 严重 | 该类的字段/getter 在**其他类**中被访问 **> 10 次** |
| 🟡 中等 | 满足触发条件但外部访问 ≤ 10 次 |
| 排除类型 | `enum`、`interface`、`abstract class`、`record`、内部匿名类 |
| 排除命名 | 类名匹配 `*DTO` / `*VO` / `*PO` / `*Entity` / `*Request` / `*Response` |
| 排除注解 | 带 `@Entity` / `@Data` / `@Value` / `@Embeddable` |

### 规模自适应

| 项目总行数 | 处理方式 |
|---|---|
| < 80 行 | **不评分**，仅列出发现，标注"项目过小，结论不可靠" |
| 80 – 500 行 | 标准阈值 |
| > 500 行 | 阈值 × 1.5（R1 严重线升至 5 次，R2 外部访问线升至 15 次） |

### 阶段 0 不输出总分

只有两条规则，加权总分没有意义，容易造成"分数看起来很科学"的假象。
阶段 0 只输出**各规则的检出清单 + 检出数量**。总分留到六条规则齐备的阶段 1。

---

## 三、预测（在跑之前写下）

### A 组 · 对照组（公认优秀的开源代码）

| 样本 | 预测 R1 组数 | 预测 R2 个数 |
|---|---|---|
| Guava `com.google.common.base` | ≤ 2 | ≤ 1 |
| JUnit5 `junit-jupiter-api` | ≤ 2 | ≤ 1 |

### B 组 · 实验组（真实学生作业）

| 样本 | 预测 R1 组数 | 预测 R2 个数 |
|---|---|---|
| `quanshr/Object-Oriented-Programming`（北航） | 1 – 8 | 1 – 6 |
| `lr580/java_homework` | 1 – 8 | 1 – 6 |
| `BitComing/Java_Homework_miniGame` | 0 – 3 | 0 – 3 |
| `Vinamdar70/Java-OOP-Assignments` | 0 – 5 | 1 – 6 |

---

## 四、证伪条件（触发即判定失败）

| 编号 | 条件 | 含义 |
|---|---|---|
| **F1** | A 组任一样本 R1 ≥ 5 组 **或** R2 ≥ 5 个 | **误报好代码**——工具的生死线，触发即推倒重来 |
| **F2** | A 组平均检出数 **≥** B 组平均检出数 | **无区分度**——工具没有测出任何有意义的东西，项目应停止 |
| **F3** | B 组全部样本 R1 = 0 且 R2 = 0 | 规则过严，什么都测不出来 |
| **F4** | B 组样本之间检出数量几乎无差异 | 无法区分代码好坏，作为教学工具无意义 |

**F1 和 F2 是硬性终止条件。** 触发后不允许通过"调整阈值让结果好看"来挽救——那正是本文件要防止的事。

---

## 五、除数字之外的判定

即使全部预测命中，还要回答一个无法量化的问题：

> **把这份报告给一个真实学生看，他的反应是「哦」还是「卧槽」？**

如果只是「哦」，那么数字再漂亮，这个产品也没有价值。

---

## 六、环境快照

| 项 | 值 |
|---|---|
| JDK | 24.0.1（Oracle，`D:\Java`） |
| Maven | 3.9.16（`C:\Users\gx293\tools\apache-maven-3.9.16`） |
| 解析器 | JavaParser（版本待定，将在 pom.xml 中锁定） |
| 操作系统 | Windows 11 amd64 |
| Maven 镜像 | 未配置，直连 Maven Central |
